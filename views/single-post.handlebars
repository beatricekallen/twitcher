<section id="all-posts">
  <h1 id="blog-title">The Twitcher Blog</h1>
  <div class="row post">
    <div class="col s12 m9 l6 post-container">
      <div class="card post-card">
        <div class="card-content white-text">
          <span class="card-title">{{post.title}}</span>
          <span class="post-author">{{user.username}}
            on
            {{format_date post.createdAt}}</span>
          <ul class="bird-list">
            <li>
              {{post.birds}}
            </li>
          </ul>
          <p>{{post.body}}</p>
        </div>
        <div class="card-action">
          <div class="comments">
            {{#each post.comments as |comment|}}
              <section class="comment">
                <div class="meta">
                  {{user.username}}
                  on
                  {{format_date createdAt}}:
                  {{comment.comment_text}}
                </div>
              </section>
            {{/each}}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<form class="comment-form" id="new-comment-form">
  <div>

    <textarea name="comment-body"></textarea>
  </div>
  <div>
    <button type="submit" name="comment-body">add comment</button>
  </div>
</form>

<script src="/js/comment.js"></script>